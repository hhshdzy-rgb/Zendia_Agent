{
  "permissions": {
    "allow": [
      "PowerShell($conn = Get-NetTCPConnection -LocalPort 8080 -ErrorAction SilentlyContinue | Select-Object -First 1; if \\($conn\\) { Get-Process -Id $conn.OwningProcess | Format-Table Id, ProcessName, StartTime, Path } else { \"port 8080 is free\" })"
    ]
  }
}
